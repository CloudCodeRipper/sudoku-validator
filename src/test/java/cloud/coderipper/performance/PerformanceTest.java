package cloud.coderipper.performance;

import org.junit.jupiter.api.Test;

import java.io.FileNotFoundException;

public class PerformanceTest {

    @Test
    void sudokuValidationPerformance() throws FileNotFoundException {
//        Instant start = Instant.now();
//
//        for (int i = 0; i<1000; i++){
//            Board board = new Board();
//            File file = FileUtils.getResourceFile("correctSudoku.csv", board.getClass());
//            int[][] matrix = board.readMatrixFromFile(file);
//            int[] column = board.getColumn(matrix, 0);
//        }
//
//        Instant finish = Instant.now();
//        long timeElapsed = Duration.between(start, finish).toMillis();  //in millis
//        System.out.println("Execution time: " + timeElapsed);
    }
}
